# ---- Site ----
NEXT_PUBLIC_SITE_URL=https://moeezmalik.site

# ---- GitHub (public data feed, no auth strictly required but recommended to raise rate limits) ----
GITHUB_USERNAME=moeezmalik10
GITHUB_TOKEN=

# ---- Groq (chat model for the RAG chatbot — free, no billing required) ----
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY=

# ---- OpenAI (OPTIONAL — only needed for embeddings if you wire up the
# Supabase vector-search path in scripts/seed-embeddings.ts; not required
# for the chatbot to work at all, since it falls back to keyword search) ----
OPENAI_API_KEY=

# ---- Supabase (vector store for RAG + contact_submissions log) ----
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ---- Resend (contact form emails) ----
RESEND_API_KEY=
CONTACT_TO_EMAIL=malikmoeez152@gmail.com
CONTACT_FROM_EMAIL=onboarding@resend.dev
